#include <stdio.h>
#include <stdbool.h>
#include <stdlib.h>

#define MOD (1e7 + 4321)

int main() {
    int n = 0, k = 0, prev = 0, cur = 0, next = 0;
    if (scanf("%d %d %d %d", &n, &k, &prev, &cur) != 4) return 2;

    while (--k) {

    }

    return 0;
}
